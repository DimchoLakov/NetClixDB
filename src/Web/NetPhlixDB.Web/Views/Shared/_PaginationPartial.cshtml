<div class="text-center">
    <ul class="pagination justify-content-center">

        @if (Model.CurrentPage - 1 < 1)
        {
            <li class="page-item disabled">
                <a class="page-link" asp-area="" asp-controller="Movies" asp-action="All" asp-route-currentPage="@(Model.CurrentPage - 1)" asp-route-search="@Model.Search" asp-route-genre="@Model.Genre" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
        }
        else
        {
            <li class="page-item">
                <a class="page-link" asp-area="" asp-controller="Movies" asp-action="All" asp-route-currentPage="@(Model.CurrentPage - 1)" asp-route-search="@Model.Search" asp-route-genre="@Model.Genre" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                    <span class="sr-only">Previous</span>
                </a>
            </li>
        }

        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            if (i == Model.CurrentPage)
            {
                <li class="page-item active">
                    <a class="page-link disabled">@i.ToString()</a>
                </li>
            }
            else
            {
                <li class="page-item">
                    <a class="page-link" asp-area="" asp-controller="Movies" asp-action="All" asp-route-currentPage="@i" asp-route-search="@Model.Search" asp-route-genre="@Model.Genre">@i.ToString()</a>
                </li>
            }
        }
        @if (Model.CurrentPage + 1 > Model.TotalPages)
        {
            <li class="page-item disabled">
                <a class="page-link" asp-area="" asp-controller="Movies" asp-route-currentPage="@(Model.CurrentPage + 1)" asp-route-search="@Model.Search" asp-route-genre="@Model.Genre" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
        }
        else
        {
            <li class="page-item">

                <a class="page-link" asp-area="" asp-controller="Movies" asp-route-currentPage="@(Model.CurrentPage + 1)" asp-route-search="@Model.Search" asp-route-genre="@Model.Genre" aria-label="Next">
                    <span aria-hidden="true">&raquo;</span>
                    <span class="sr-only">Next</span>
                </a>
            </li>
        }
    </ul>
</div>